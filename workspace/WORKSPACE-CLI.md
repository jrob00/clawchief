# Workspace CLI

Google Workspace CLI: gws

Use `gws` (Google's official Workspace CLI) for all Google Workspace operations including Gmail, Calendar, and Sheets.

Prefer `gws` agent skills (prefixed with `+`) over raw discovery commands when available.
